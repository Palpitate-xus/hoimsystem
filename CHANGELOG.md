# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [未发布]

### 修复 — 性能基准

- 将初始化器和基准服务器固定到仓库内专用的 `fastapi_be/benchmark.db`，忽略外部 `DATABASE_URL`；初始化时重建 schema，避免误清理其他数据库及旧 schema 残留
- SQLite 基准改用 `QueuePool` 为并发会话提供独立连接，并确保 `run_benchmark:app` 可由 Uvicorn 正常加载
- Locust 在每轮测试开始前通过 `/api/login` 获取并检查新 token；认证失败、token 过期或剩余有效期不足时立即终止测试
- 混合场景使用 `admin`、`doctor`、`patient` 分角色身份，读取实际业务对象构造写入目标；预约和处方同时检查 HTTP 状态与响应体 `code == 200`

### 文档

- 撤回旧 `StaticPool`、预生成 token 和错误业务成功判定下生成的基准结果；修复后的性能基线待重新测量

## [2.0.0] - 2026-08-23

> 本版本为重大更新（MAJOR）：系统规模从 61 表/247 接口跃升至 **139 表/540 接口**，新增 17 个 HIS 业务模块；
> 安全基线全面加固，生产部署要求提升（强制 SECRET_KEY 环境变量）。

### 破坏性变更

- 生产环境（`ENV=production`）启动时**强制校验 `SECRET_KEY`**，缺失或为默认值将拒绝启动——升级部署前必须配置
- 数据库迁移链延伸至 `20260823_home_icd`，升级时需执行 `alembic upgrade head`
- 病案借阅由"直接借出"改为**审批流**（申请→审批→借出），依赖旧借阅行为的集成需适配

### 新增 — HIS 补齐模块（四批交付，17 个模块，零预置数据）

- **审方规则引擎**：药师维护配伍禁忌/禁忌/剂量范围/重复用药/过敏关键词 5 类规则 × 3 级严重度；**禁止级规则双向阻断开方与药师发药**；未配置规则时零影响
- **医保目录对照**：本院 8 类收费项目 ↔ 医保目录映射（甲乙丙类/自付比例/限价），Excel 模板下载 + 幂等批量导入
- **CSSD 消毒供应**：待回收→清洗→打包→灭菌→无菌→发放→回收全状态机；BD 试验未过不能灭菌，生物监测 + 无菌效期双门槛才能置"无菌可用"
- **PIVAS 静配中心**：排药→贴签→配置→成品核对→配送→病区签收批次流；**成品核对禁止调配人本人操作（双人复核硬约束）**；细胞毒/肠外营养标记
- **ICU/PACU 专科评分**：服务端汇总 APACHE II（0-71）/SOFA（0-24）/GCS（3-15 带意识障碍分级）/Aldrete（≥9 达转出）/Steward（≥4 达转出）
- **临床路径执行**：入组（同患者仅一条活动记录）/进度登记/变异四分类（病情/医方/患方/系统）/完成出径强制全部节点完成、退出必填原因
- **MDRO 隔离**：耐药菌隔离登记/解除（同患者同菌种防重，解除日期校验），床头隔离标识
- **手卫生监测**：五时刻观察录入，应执行/实际执行校验，依从率自动计算
- **传染病报告卡**：法定 40+ 病种**自动判甲/乙/丙类**；待上报→网直上报（回填卡号）→审核（仅管理员）→订正（重新判类）闭环
- **不良事件 RCA**：一事件一分析；时间线还原 + 根因/改进措施；**PDCA 只能按 P→D→C→A 顺序推进**，A 阶段强制效果评价
- **HQMS 质量指标**：分子/分母自动算值（% 换算），同期间同科室唯一，粘贴批量导入 + 批量上报
- **病案借阅审批流**：申请（待审批）→病案管理员批准借出/驳回（必填原因）→归还重置审批流；审批工作台
- **病案 ICD 编码工作台**：文本诊断/手术 → ICD 码绑定（字典校验、编码大写归一、主诊断唯一互斥）；待编码工作台 + 覆盖率统计 + 主诊断 TOP10
- **科室绩效核算**：工作量明细（数量×单价或小计）与成本分摊服务端求和，绩效 =(工作量−成本)×系数（负绩效如实保留）；草稿→提交锁定→审核发放/退回
- **围术期抗菌药依从统计**：切皮前 30-120 分钟给药判依从（0.5-2h 原则），总体/过早/过晚/按手术级别分布
- **急诊绿色通道计费闭环**：先救治后收费——关闭通道时自动补记待计费留观费用生成 Charge
- **日结缴费日口径**：`/dailySettlement/byPayDate` 按 `Payment.paid_time` 收付实现制统计，总收入/退费/净收入/按渠道拆分，解决开单日与缴费日跨日口径偏差
- **报损批次台账**：报损审批通过后按 **FEFO 逐批冲减**并写批次台账；批次合计不足即拒绝（总量够但批次不够防串账）

### 新增 — 安全加固

- 登录令牌吊销（登出即失效）+ 持久化失败锁定
- RBAC 漂移防护：矩阵自动生成（`scripts/generate_rbac_matrix.py`），CI 校验路由声明与矩阵一致
- 注入面收口：文件处理审计后的最终加固（上传校验/导出净化/导入校验）
- CSRF Origin 校验中间件 + GZip + 安全响应头
- 凭据治理：移除遗留配置备份中的凭据；文档默认账号改由首次启动注入
- 等保基线自查文档（`fastapi_be/SECURITY_AUDIT.md`）

### 新增 — 前端

- 审方规则引擎页（规则维护 + 处方实时预检）、医保目录对照页（手工录入 + 模板下载 + 粘贴导入）
- MDRO 隔离页、传染病报告卡页、质量管理页（RCA + HQMS + 抗菌药依从三 Tab）
- 运营扩展页（CSSD/PIVAS/评分/路径四 Tab）、ICD 编码工作台页
- 病案归档页内嵌借阅审批工作台；日结对账页新增开单日/缴费日口径切换；科室绩效核算页
- 页面总数 69 → **143**

### 修复 — 业务审计（61 项）

- 住院计费：费用漏记/重复计费/结算时序问题
- 药房：批次 FEFO 出库、退药回冲批次账、麻精双人复核缺口
- 用血安全：血型校验与输血闭环缺口
- 收费闭环：费用状态与发票联动缺口
- LIS 危急值：四步闭环完整性
- 感染列表性能、急诊留观费用守卫、窗口挂号费一致性等

### 修复 — 安全（31 项）

越权访问（IDOR）、注入防护、敏感数据暴露、访问控制缺口等，详见 `fastapi_be/SECURITY_AUDIT.md`

### 性能

- 感染列表查询优化（N+1 消除）
- 处方/费用列表分页与索引

### 测试

- 测试规模 438 → **1066 passed / 3 skipped**（新增 600+ 用例：并发安全、金额精度、状态机、越权、注入防护、HIS 模块）
- RBAC 专项 636 用例逐端点验证 401/403
- Lint 基线 188（低于历史基线 192）

### 文档

- 新增 `doc/his-feature-gap-analysis.md`（HIS 完整性盘点 + 四批交付记录）
- 新增 `doc/flowcharts/09-HIS补齐模块.md`（15 张流程图）
- README（中英）规模与特性全量同步；软件手册/数据库文档/数据字典/RBAC 矩阵同步 540 接口 139 表

## [1.1.0] - 2026-05-18

### 新增

- **住院全流程**：病区床位管理、入院登记、住院医嘱（长期/临时）、护士工作站、住院费用、出院结算
- **手术麻醉管理**：手术申请、手术排台、麻醉记录
- **电子病历进阶**：结构化病历、病程记录、查房记录、病历模板、CA 数字签名
- **体检中心**：体检套餐、项目管理、体检预约、记录录入、报告生成
- **多学科会诊（MDT）**：会诊申请、参与科室管理、结果录入
- **临床路径**：路径模板、患者路径跟踪
- **双向转诊**：门诊/住院/急诊转诊申请、处理流程
- **预交金账户**：充值、查询、扣费、退款
- **耗材管理**：医疗耗材入库、查询、修改、删除
- **药品采购**：采购订单创建、审批、入库
- **ADR 监测**：药品不良反应上报、跟踪、处理
- **不良事件上报**：医疗安全事件记录
- **分诊台管理**：4 级分诊、科室分流
- **候诊巡视**：巡视记录、关注高风险患者
- **复诊随访**：随访计划、患者反馈
- **操作日志自动记录**：FastAPI 中间件拦截 POST/PUT/DELETE 操作，自动写入审计日志

### 优化

- **操作日志中间件**：从 accesstoken 解析用户、路径自动映射为中文操作+对象
- **响应时间脱敏**：StripMicrosecondMiddleware 自动去除 datetime 微秒部分
- **身份证号脱敏**：前端 maskIdentity 函数统一脱敏显示（370101********1234）
- **三步式业务流程**：现场挂号、报到签到、双向转诊改为三步式（输入身份证→选项卡片→成功）
- **UI 全面美化**：
  - 移除 30+ 页面表格中的 ID/UUID 列
  - 所有 el-table 添加 empty-text 空状态
  - 所有列表加 v-loading 加载状态
  - 表单手动输入 ID 改为 el-select 下拉
  - 状态列统一使用 el-tag 美化
  - 搜索框 placeholder 改为具体提示
- **文字表述优化**：移除模板自带的"vue-admin-better"等字样，统一为医疗系统术语
- **首页快捷入口**：根据用户角色动态显示常用功能入口
- **通知公告点击跳转**：首页公告时间线可点击查看详情

### 修复

- 修复 main.py 中重复创建 FastAPI 实例的 bug
- 修复前端挂载点 ID 不一致导致的空白页（index.html ↔ main.js）
- 修复 vab.scss 注释格式语法错误
- 修复 admin/doctor1 测试账号缺失关联数据
- 修复 MDT 会诊显示科室 ID 而非名称
- 修复 birthday/exam_date 等日期字段错误显示时分秒
- 修复 ExamResult.doctor 关联导致的 SQLAlchemy 错误
- 修复多个页面 API 导入名错误（getScheduleList、getAppointmentsForCheckin）
- 过滤 rspack dev server 的 ResizeObserver 无害运行时错误

### 文档

- 新增 `doc/README.md` 文档导航中心
- 新增 `doc/architecture.md` 系统架构文档
- 新增 `doc/user-manual.md` 用户操作手册（650+ 行，覆盖所有角色）
- 更新主 README.md：补充真实统计数据（247 API、61 表、34 模块、69 页面）
- 更新 deployDoc.md 端口号说明

---

## [1.0.0] - 2026-05-10

### 新增

- **门诊全流程管理**：覆盖挂号、就诊、收费、取药、检验、随访等门诊完整流程
- **多角色支持**：管理员、科室主任、医生、护士、收费员、患者、导医/挂号员
- **预约挂号**：在线预约医生号源，支持现场挂号和窗口挂号
- **排队叫号**：候诊队列管理、叫号、过号、跳号、急诊优先
- **电子病历**：病历书写、查询、修改，关联挂号/预约记录
- **处方管理**：医生开处方、库存校验、处方审核、发药确认、退药处理
- **收费管理**：费用计算、在线缴费、退费、发票管理、日结对账
- **检查检验**：检查申请、结果录入、危急值报告、样本全流程跟踪
- **药品管理**：药品入库、查询、库存预警、库存盘点、处方点评
- **智能导诊**：310+ 症状关键词匹配，推荐科室和医生
- **报表统计**：门诊量、财务、药品消耗、医生工作量多维统计
- **消息中心**：站内信/短信通知、消息列表、已读标记
- **支付接口**：微信/支付宝 Mock 支付、现金支付、支付流水
- **权限分配**：用户角色管理、密码重置、用户删除
- **医生考勤**：每日签到/签退、出勤统计
- **数据备份恢复**：手动备份、恢复、下载、删除
- **操作日志审计**：全量操作记录、筛选查询
- **系统管理**：数据字典、系统参数配置
- **前端页面美观性**：统一搜索栏、分页、表单样式，响应式布局

### 安全

- 密码加密存储（bcrypt）
- JWT Token 认证替代纯字符串 Token
- 密码传输 RSA 加密
- 接口角色权限校验细化
- 敏感配置项改为环境变量注入

### 基础设施

- Docker 支持（Dockerfile + docker-compose.yml）
- GitHub Actions CI/CD 工作流
- 部署文档（deployDoc.md）
- 贡献指南（CONTRIBUTING.md）
- 开源许可证（MIT）

---

## 历史开发阶段

### Phase 12 - 权限分配
- 用户权限管理 API（角色分配、菜单权限、数据权限）
- 权限管理页面

### Phase 11 - 数据备份恢复
- 数据库备份/恢复/下载/删除 API
- 数据备份恢复页面

### Phase 10 - 智能导诊
- 症状→科室映射（310+ 关键词）
- 智能导诊页面

### Phase 9 - 支付接口
- 微信/支付宝 Mock 支付
- 现金支付、支付流水查询

### Phase 8 - 消息中心
- 站内信/短信通知接口
- 消息列表、已读标记

### Phase 7 - 检验科样本管理
- 样本接收/拒收、流转跟踪
- 检验结果录入、危急值报告

### Phase 6 - 收费员角色
- 窗口挂号、退号、收费、退费
- 日结对账、预交金管理

### Phase 5 - 药房扩展
- 处方审核、发药确认、退药处理
- 库存盘点、处方点评

### Phase 4 - 护士角色扩展
- 候诊队列管理、叫号显示
- 生命体征录入

### Phase 3 - 医生考勤签到
- 每日签到/签退 API
- 考勤记录查询

### Phase 2 - 违约处理
- 三次未取号暂停预约资格
- 违约记录查询

### Phase 1 - 库存预警
- 低库存/近效期药品自动提醒
- 库存预警页面
